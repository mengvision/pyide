# Server API base URL.
# In development (npm run dev) the Vite dev server proxies /api and /ws
# to this URL automatically, so an empty string also works for dev.
# In Docker the nginx container proxies directly to the 'api' service.
VITE_API_URL=http://localhost:8000
